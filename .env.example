GEMINI_API_KEY=your_gemini_api_key_here

# GPU selection (optional) - use UUID from `nvidia-smi -L`
# Leave empty to use default GPU
CUDA_DEVICE=

# Claude CLI working directory (optional)
# Defaults to ~/dev if not set
CLAUDE_WORK_DIR=
